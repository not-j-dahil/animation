"# animation" 
from tkinter import *
import random
import time

root = Tk()

HEIGHT = 500
WIDTH = 500

canvas = Canvas(root, width=WIDTH, height=HEIGHT)
root.title("Graphics and Animation")
canvas.pack()
